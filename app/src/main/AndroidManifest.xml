<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.localapproval">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.localapproval.FinishedOrderListActivity"></activity>
        <activity android:name="com.example.localapproval.ShopDetailsActivity" />
        <activity android:name="com.example.localapproval.DetailsOrderActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.example.localapproval.MapsActivity"
            android:label="location" />
        <activity android:name="com.example.localapproval.tab3Activity" />
        <activity android:name="com.example.localapproval.tab2Activity" />
        <activity android:name="com.example.localapproval.tab1Activity" />
        <activity android:name="com.example.localapproval.detailsActivity" />
        <activity
            android:name="com.example.localapproval.deliveryActivity"
            android:label="delivery accounts" />
        <activity
            android:name="com.example.localapproval.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>


</manifest>